{
  "name": "skill-forge",
  "version": "0.1.0",
  "type": "module",
  "description": "Claude Code plugin that autonomously generates skills from PRDs, tech specs, and codebases",
  "scripts": {
    "start": "bun run mcp-server/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
